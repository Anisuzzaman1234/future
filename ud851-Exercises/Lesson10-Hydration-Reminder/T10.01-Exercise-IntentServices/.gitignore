.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.01-Exercise-IntentServices/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.01-Exercise-IntentServices/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,5 +19,5 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
} 
  
5  Lesson10-Hydration-Reminder/T10.01-Exercise-IntentServices/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}

  
4  ...ydration-Reminder/T10.01-Exercise-IntentServices/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
#Fri Jan 13 11:41:03 PST 2017
#Mon Sep 25 13:19:01 AEST 2017
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-3.3-all.zip
distributionUrl=https\://services.gradle.org/distributions/gradle-3.4-all.zip
  
127  Lesson10-Hydration-Reminder/T10.01-Solution-IntentServices/.gitignore
@@ -1,124 +1,41 @@
.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.01-Solution-IntentServices/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.01-Solution-IntentServices/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,5 +19,5 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
} 
  
5  Lesson10-Hydration-Reminder/T10.01-Solution-IntentServices/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}

  
4  ...ydration-Reminder/T10.01-Solution-IntentServices/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
#Fri Jan 13 11:41:03 PST 2017
#Mon Sep 25 13:19:01 AEST 2017
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-3.3-all.zip
distributionUrl=https\://services.gradle.org/distributions/gradle-3.4-all.zip
  
127  Lesson10-Hydration-Reminder/T10.02-Exercise-CreateNotification/.gitignore
@@ -1,124 +1,41 @@
.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.02-Exercise-CreateNotification/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.02-Exercise-CreateNotification/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,5 +19,5 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
} 
  
4  ...10-Hydration-Reminder/T10.02-Exercise-CreateNotification/app/src/main/AndroidManifest.xml
@@ -1,14 +1,14 @@
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.background">
    <!-- TODO (10) Add the VIBRATE permission -->
    <!-- TODO (13) Add the VIBRATE permission -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- TODO (15) Add the launch mode "single top" attribute to the MainActivity so that when you open
        <!-- TODO (16) Add the launch mode "single top" attribute to the MainActivity so that when you open
        MainActivity using a notification, it will take you back to the already opened activity, not
        generate a new one -->
        <activity
  
2  ...ise-CreateNotification/app/src/main/java/com/example/android/background/MainActivity.java
@@ -89,7 +89,7 @@ public void incrementWater(View view) {
        startService(incrementWaterCountIntent);
    }

    // TODO (14) Create a method called testNotification that triggers NotificationUtils' remindUserBecauseCharging
    // TODO (15) Create a method called testNotification that triggers NotificationUtils' remindUserBecauseCharging

    @Override
    protected void onDestroy() {
  
9  ...ication/app/src/main/java/com/example/android/background/utilities/NotificationUtils.java
@@ -9,7 +9,9 @@
    // This method will create a notification for charging. It might be helpful
    // to take a look at this guide to see an example of what the code in this method will look like:
    // https://developer.android.com/training/notify-user/build-notification.html
        // TODO (8) In the remindUser method use NotificationCompat.Builder to create a notification
        // TODO (8) Get the NotificationManager using context.getSystemService
        // TODO (9) Create a notification channel for Android O devices
        // TODO (10) In the remindUser method use NotificationCompat.Builder to create a notification
        // that:
        // - has a color of R.colorPrimary - use ContextCompat.getColor to get a compatible color
        // - has ic_drink_notification as the small icon
@@ -20,9 +22,8 @@
        // - sets the notification defaults to vibrate
        // - uses the content intent returned by the contentIntent helper method for the contentIntent
        // - automatically cancels the notification when the notification is clicked
        // TODO (9) If the build version is greater than JELLY_BEAN, set the notification's priority
        // to PRIORITY_HIGH.
        // TODO (11) Get a NotificationManager, using context.getSystemService(Context.NOTIFICATION_SERVICE);
        // TODO (11) If the build version is greater than JELLY_BEAN and lower than OREO,
        // set the notification's priority to PRIORITY_HIGH.
        // TODO (12) Trigger the notification by calling notify on the NotificationManager.
        // Pass in a unique ID of your choosing for the notification and notificationBuilder.build()

  
2  ...ion-Reminder/T10.02-Exercise-CreateNotification/app/src/main/res/layout/activity_main.xml
@@ -85,5 +85,5 @@
            android:text="@string/default_charge_notification_count" />

    </RelativeLayout>
    <!-- TODO (13) Add a button for testing your notification with an onClick method called testNotification -->
    <!-- TODO (14) Add a button for testing your notification with an onClick method called testNotification -->
</LinearLayout> 
  
5  Lesson10-Hydration-Reminder/T10.02-Exercise-CreateNotification/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}

  
4  ...tion-Reminder/T10.02-Exercise-CreateNotification/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
#Fri Jan 13 11:41:03 PST 2017
#Mon Sep 25 13:19:01 AEST 2017
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-3.3-all.zip
distributionUrl=https\://services.gradle.org/distributions/gradle-3.4-all.zip
  
127  Lesson10-Hydration-Reminder/T10.02-Solution-CreateNotification/.gitignore
@@ -1,124 +1,41 @@
.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.02-Solution-CreateNotification/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.02-Solution-CreateNotification/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,5 +19,5 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
} 
  
4  ...10-Hydration-Reminder/T10.02-Solution-CreateNotification/app/src/main/AndroidManifest.xml
@@ -1,15 +1,15 @@
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.background">
    <!-- COMPLETED (10) Add the VIBRATE permission -->
    <!-- COMPLETED (13) Add the VIBRATE permission -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- COMPLETED (15) Add the launch mode "single top" attribute to the MainActivity so that when you open
        <!-- COMPLETED (16) Add the launch mode "single top" attribute to the MainActivity so that when you open
        MainActivity using a notification, it will take you back to the already opened activity, not
        generate a new one -->
        <activity
  
2  ...ion-CreateNotification/app/src/main/java/com/example/android/background/MainActivity.java
@@ -90,7 +90,7 @@ public void incrementWater(View view) {
        startService(incrementWaterCountIntent);
    }

    // COMPLETED (14) Create a method called testNotification that triggers NotificationUtils' remindUserBecauseCharging
    // COMPLETED (15) Create a method called testNotification that triggers NotificationUtils' remindUserBecauseCharging
    public void testNotification(View view) {
        NotificationUtils.remindUserBecauseCharging(this);
    }
  
33  ...ication/app/src/main/java/com/example/android/background/utilities/NotificationUtils.java
@@ -16,6 +16,7 @@
package com.example.android.background.utilities;

import android.app.Notification;
import android.app.NotificationChannel;
import android.app.NotificationManager;
import android.app.PendingIntent;
import android.content.Context;
@@ -45,13 +46,28 @@
     * This pending intent id is used to uniquely reference the pending intent
     */
    private static final int WATER_REMINDER_PENDING_INTENT_ID = 3417;
    /**
     * This notification channel id is used to link notifications to this channel
     */
    private static final String WATER_REMINDER_NOTIFICATION_CHANNEL_ID = "reminder_notification_channel";

    // COMPLETED (7) Create a method called remindUserBecauseCharging which takes a Context.
    // This method will create a notification for charging. It might be helpful
    // to take a look at this guide to see an example of what the code in this method will look like:
    // https://developer.android.com/training/notify-user/build-notification.html
    public static void remindUserBecauseCharging(Context context) {
        // COMPLETED (8) In the remindUser method use NotificationCompat.Builder to create a notification
        // COMPLETED (8) Get the NotificationManager using context.getSystemService
        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);
        // COMPLETED (9) Create a notification channel for Android O devices
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            NotificationChannel mChannel = new NotificationChannel(
                    WATER_REMINDER_NOTIFICATION_CHANNEL_ID,
                    context.getString(R.string.main_notification_channel_name),
                    NotificationManager.IMPORTANCE_HIGH);
            notificationManager.createNotificationChannel(mChannel);
            }
        // COMPLETED (10) In the remindUser method use NotificationCompat.Builder to create a notification
        // that:
        // - has a color of R.colorPrimary - use ContextCompat.getColor to get a compatible color
        // - has ic_drink_notification as the small icon
@@ -62,7 +78,7 @@ public static void remindUserBecauseCharging(Context context) {
        // - sets the notification defaults to vibrate
        // - uses the content intent returned by the contentIntent helper method for the contentIntent
        // - automatically cancels the notification when the notification is clicked
        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context)
        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context,WATER_REMINDER_NOTIFICATION_CHANNEL_ID)
                .setColor(ContextCompat.getColor(context, R.color.colorPrimary))
                .setSmallIcon(R.drawable.ic_drink_notification)
                .setLargeIcon(largeIcon(context))
@@ -74,16 +90,13 @@ public static void remindUserBecauseCharging(Context context) {
                .setContentIntent(contentIntent(context))
                .setAutoCancel(true);

        // COMPLETED (9) If the build version is greater than JELLY_BEAN, set the notification's priority
        // to PRIORITY_HIGH.
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN) {
            notificationBuilder.setPriority(Notification.PRIORITY_HIGH);
        // COMPLETED (11) If the build version is greater than JELLY_BEAN and lower than OREO,
        // set the notification's priority to PRIORITY_HIGH.
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN
                && Build.VERSION.SDK_INT < Build.VERSION_CODES.O) {
            notificationBuilder.setPriority(NotificationCompat.PRIORITY_HIGH);
        }

        // COMPLETED (11) Get a NotificationManager, using context.getSystemService(Context.NOTIFICATION_SERVICE);
        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);

        // COMPLETED (12) Trigger the notification by calling notify on the NotificationManager.
        // Pass in a unique ID of your choosing for the notification and notificationBuilder.build()
        notificationManager.notify(WATER_REMINDER_NOTIFICATION_ID, notificationBuilder.build());
  
2  ...ion-Reminder/T10.02-Solution-CreateNotification/app/src/main/res/layout/activity_main.xml
@@ -86,7 +86,7 @@

    </RelativeLayout>

    <!-- COMPLETED (13) Add a button for testing your notification with an onClick method called testNotification -->
    <!-- COMPLETED (14) Add a button for testing your notification with an onClick method called testNotification -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
  
1  ...Hydration-Reminder/T10.02-Solution-CreateNotification/app/src/main/res/values/strings.xml
@@ -22,5 +22,6 @@

    <string name="charging_reminder_notification_title">Device Charging - Where\'s that water?</string>
    <string name="charging_reminder_notification_body">There\'s never a better time to drink water than when you know your phone is charging. Drink up!</string>
    <string name="main_notification_channel_name">Primary</string>

</resources>
  
5  Lesson10-Hydration-Reminder/T10.02-Solution-CreateNotification/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}

  
4  ...tion-Reminder/T10.02-Solution-CreateNotification/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
#Fri Jan 13 11:41:03 PST 2017
#Mon Sep 25 13:19:01 AEST 2017
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-3.3-all.zip
distributionUrl=https\://services.gradle.org/distributions/gradle-3.4-all.zip
  
127  Lesson10-Hydration-Reminder/T10.03-Exercise-NotificationActions/.gitignore
@@ -1,124 +1,41 @@
.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.03-Exercise-NotificationActions/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.03-Exercise-NotificationActions/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,5 +19,5 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
} 
  
1  ...0-Hydration-Reminder/T10.03-Exercise-NotificationActions/app/src/main/AndroidManifest.xml
@@ -1,6 +1,7 @@
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.background">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
  
1  ...se-NotificationActions/app/src/main/java/com/example/android/background/MainActivity.java
@@ -90,6 +90,7 @@ public void incrementWater(View view) {
        startService(incrementWaterCountIntent);
    }


    public void testNotification(View view) {
        NotificationUtils.remindUserBecauseCharging(this);
    }
  
25  ...Actions/app/src/main/java/com/example/android/background/utilities/NotificationUtils.java
@@ -16,6 +16,7 @@
package com.example.android.background.utilities;

import android.app.Notification;
import android.app.NotificationChannel;
import android.app.NotificationManager;
import android.app.PendingIntent;
import android.content.Context;
@@ -45,11 +46,24 @@
     * This pending intent id is used to uniquely reference the pending intent
     */
    private static final int WATER_REMINDER_PENDING_INTENT_ID = 3417;
    /**
     * This notification channel id is used to link notifications to this channel
     */
    private static final String WATER_REMINDER_NOTIFICATION_CHANNEL_ID = "reminder_notification_channel";

    //  TODO (1) Create a method to clear all notifications

    public static void remindUserBecauseCharging(Context context) {
        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context)
        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            NotificationChannel mChannel = new NotificationChannel(
                    WATER_REMINDER_NOTIFICATION_CHANNEL_ID,
                    context.getString(R.string.main_notification_channel_name),
                    NotificationManager.IMPORTANCE_HIGH);
            notificationManager.createNotificationChannel(mChannel);
            }
        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context,WATER_REMINDER_NOTIFICATION_CHANNEL_ID)
                .setColor(ContextCompat.getColor(context, R.color.colorPrimary))
                .setSmallIcon(R.drawable.ic_drink_notification)
                .setLargeIcon(largeIcon(context))
@@ -62,13 +76,10 @@ public static void remindUserBecauseCharging(Context context) {
                // TODO (17) Add the two new actions using the addAction method and your helper methods
                .setAutoCancel(true);

        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN) {
            notificationBuilder.setPriority(Notification.PRIORITY_HIGH);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN
                && Build.VERSION.SDK_INT < Build.VERSION_CODES.O) {
            notificationBuilder.setPriority(NotificationCompat.PRIORITY_HIGH);
        }

        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);

        notificationManager.notify(WATER_REMINDER_NOTIFICATION_ID, notificationBuilder.build());
    }

  
1  ...on-Reminder/T10.03-Exercise-NotificationActions/app/src/main/res/layout/activity_main.xml
@@ -86,6 +86,7 @@

    </RelativeLayout>


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
  
1  ...ydration-Reminder/T10.03-Exercise-NotificationActions/app/src/main/res/values/strings.xml
@@ -22,5 +22,6 @@

    <string name="charging_reminder_notification_title">Device Charging - Where\'s that water?</string>
    <string name="charging_reminder_notification_body">There\'s never a better time to drink water than when you know your phone is charging. Drink up!</string>
    <string name="main_notification_channel_name">Primary</string>

</resources>
  
5  Lesson10-Hydration-Reminder/T10.03-Exercise-NotificationActions/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}

  
4  ...ion-Reminder/T10.03-Exercise-NotificationActions/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
#Fri Jan 13 11:41:03 PST 2017
#Mon Sep 25 13:19:01 AEST 2017
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-3.3-all.zip
distributionUrl=https\://services.gradle.org/distributions/gradle-3.4-all.zip
  
127  Lesson10-Hydration-Reminder/T10.03-Solution-NotificationActions/.gitignore
@@ -1,124 +1,41 @@
.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.03-Solution-NotificationActions/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.03-Solution-NotificationActions/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,5 +19,5 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
} 
  
1  ...0-Hydration-Reminder/T10.03-Solution-NotificationActions/app/src/main/AndroidManifest.xml
@@ -1,6 +1,7 @@
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.background">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
  
1  ...on-NotificationActions/app/src/main/java/com/example/android/background/MainActivity.java
@@ -90,6 +90,7 @@ public void incrementWater(View view) {
        startService(incrementWaterCountIntent);
    }


    public void testNotification(View view) {
        NotificationUtils.remindUserBecauseCharging(this);
    }
  
28  ...Actions/app/src/main/java/com/example/android/background/utilities/NotificationUtils.java
@@ -16,6 +16,7 @@
package com.example.android.background.utilities;

import android.app.Notification;
import android.app.NotificationChannel;
import android.app.NotificationManager;
import android.app.PendingIntent;
import android.content.Context;
@@ -49,6 +50,10 @@
     * This pending intent id is used to uniquely reference the pending intent
     */
    private static final int WATER_REMINDER_PENDING_INTENT_ID = 3417;
    /**
     * This notification channel id is used to link notifications to this channel
     */
    private static final String WATER_REMINDER_NOTIFICATION_CHANNEL_ID = "reminder_notification_channel";
    private static final int ACTION_DRINK_PENDING_INTENT_ID = 1;
    private static final int ACTION_IGNORE_PENDING_INTENT_ID = 14;

@@ -60,8 +65,16 @@ public static void clearAllNotifications(Context context) {
    }

    public static void remindUserBecauseCharging(Context context) {

        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context)
        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            NotificationChannel mChannel = new NotificationChannel(
                    WATER_REMINDER_NOTIFICATION_CHANNEL_ID,
                    context.getString(R.string.main_notification_channel_name),
                    NotificationManager.IMPORTANCE_HIGH);
            notificationManager.createNotificationChannel(mChannel);
            }
        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context,WATER_REMINDER_NOTIFICATION_CHANNEL_ID)
                .setColor(ContextCompat.getColor(context, R.color.colorPrimary))
                .setSmallIcon(R.drawable.ic_drink_notification)
                .setLargeIcon(largeIcon(context))
@@ -76,15 +89,10 @@ public static void remindUserBecauseCharging(Context context) {
                .addAction(ignoreReminderAction(context))
                .setAutoCancel(true);

        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN) {
            notificationBuilder.setPriority(Notification.PRIORITY_HIGH);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN
                && Build.VERSION.SDK_INT < Build.VERSION_CODES.O) {
            notificationBuilder.setPriority(NotificationCompat.PRIORITY_HIGH);
        }


        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);

        /* WATER_REMINDER_NOTIFICATION_ID allows you to update or cancel the notification later on */
        notificationManager.notify(WATER_REMINDER_NOTIFICATION_ID, notificationBuilder.build());
    }
    //  COMPLETED (5) Add a static method called ignoreReminderAction
  
1  ...on-Reminder/T10.03-Solution-NotificationActions/app/src/main/res/layout/activity_main.xml
@@ -86,6 +86,7 @@

    </RelativeLayout>


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
  
1  ...ydration-Reminder/T10.03-Solution-NotificationActions/app/src/main/res/values/strings.xml
@@ -22,5 +22,6 @@

    <string name="charging_reminder_notification_title">Device Charging - Where\'s that water?</string>
    <string name="charging_reminder_notification_body">There\'s never a better time to drink water than when you know your phone is charging. Drink up!</string>
    <string name="main_notification_channel_name">Primary</string>

</resources>
  
5  Lesson10-Hydration-Reminder/T10.03-Solution-NotificationActions/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}

  
4  ...ion-Reminder/T10.03-Solution-NotificationActions/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
#Fri Jan 13 11:41:03 PST 2017
#Mon Sep 25 13:19:01 AEST 2017
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-3.3-all.zip
distributionUrl=https\://services.gradle.org/distributions/gradle-3.4-all.zip
  
127  Lesson10-Hydration-Reminder/T10.04-Exercise-PeriodicSyncWithJobDispatcher/.gitignore
@@ -1,124 +1,41 @@
.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.04-Exercise-PeriodicSyncWithJobDispatcher/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.04-Exercise-PeriodicSyncWithJobDispatcher/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,6 +19,6 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
    // TODO (1) Add gradle dependency for Firebase Job Dispatcher
}
  
1  ...n-Reminder/T10.04-Exercise-PeriodicSyncWithJobDispatcher/app/src/main/AndroidManifest.xml
@@ -1,6 +1,7 @@
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.background">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
  
1  ...cSyncWithJobDispatcher/app/src/main/java/com/example/android/background/MainActivity.java
@@ -94,6 +94,7 @@ public void incrementWater(View view) {


    // TODO (24) Remove the button and testNotification code

    public void testNotification(View view) {
        NotificationUtils.remindUserBecauseCharging(this);
    }
  
28  ...patcher/app/src/main/java/com/example/android/background/utilities/NotificationUtils.java
@@ -16,6 +16,7 @@
package com.example.android.background.utilities;

import android.app.Notification;
import android.app.NotificationChannel;
import android.app.NotificationManager;
import android.app.PendingIntent;
import android.content.Context;
@@ -49,6 +50,10 @@
     * This pending intent id is used to uniquely reference the pending intent
     */
    private static final int WATER_REMINDER_PENDING_INTENT_ID = 3417;
    /**
     * This notification channel id is used to link notifications to this channel
     */
    private static final String WATER_REMINDER_NOTIFICATION_CHANNEL_ID = "reminder_notification_channel";
    private static final int ACTION_DRINK_PENDING_INTENT_ID = 1;
    private static final int ACTION_IGNORE_PENDING_INTENT_ID = 14;

@@ -59,8 +64,16 @@ public static void clearAllNotifications(Context context) {
    }

    public static void remindUserBecauseCharging(Context context) {

        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context)
        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            NotificationChannel mChannel = new NotificationChannel(
                    WATER_REMINDER_NOTIFICATION_CHANNEL_ID,
                    context.getString(R.string.main_notification_channel_name),
                    NotificationManager.IMPORTANCE_HIGH);
            notificationManager.createNotificationChannel(mChannel);
            }
        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context,WATER_REMINDER_NOTIFICATION_CHANNEL_ID)
                .setColor(ContextCompat.getColor(context, R.color.colorPrimary))
                .setSmallIcon(R.drawable.ic_drink_notification)
                .setLargeIcon(largeIcon(context))
@@ -74,15 +87,10 @@ public static void remindUserBecauseCharging(Context context) {
                .addAction(ignoreReminderAction(context))
                .setAutoCancel(true);

        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN) {
            notificationBuilder.setPriority(Notification.PRIORITY_HIGH);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN
                && Build.VERSION.SDK_INT < Build.VERSION_CODES.O) {
            notificationBuilder.setPriority(NotificationCompat.PRIORITY_HIGH);
        }


        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);

        /* WATER_REMINDER_NOTIFICATION_ID allows you to update or cancel the notification later on */
        notificationManager.notify(WATER_REMINDER_NOTIFICATION_ID, notificationBuilder.build());
    }
    private static Action ignoreReminderAction(Context context) {
  
1  ...r/T10.04-Exercise-PeriodicSyncWithJobDispatcher/app/src/main/res/layout/activity_main.xml
@@ -86,6 +86,7 @@

    </RelativeLayout>


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
  
1  ...eminder/T10.04-Exercise-PeriodicSyncWithJobDispatcher/app/src/main/res/values/strings.xml
@@ -22,5 +22,6 @@

    <string name="charging_reminder_notification_title">Device Charging - Where\'s that water?</string>
    <string name="charging_reminder_notification_body">There\'s never a better time to drink water than when you know your phone is charging. Drink up!</string>
    <string name="main_notification_channel_name">Primary</string>

</resources>
  
5  Lesson10-Hydration-Reminder/T10.04-Exercise-PeriodicSyncWithJobDispatcher/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}

  
4  ...er/T10.04-Exercise-PeriodicSyncWithJobDispatcher/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
#Fri Jan 13 11:41:03 PST 2017
#Mon Sep 25 13:19:01 AEST 2017
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-3.3-all.zip
distributionUrl=https\://services.gradle.org/distributions/gradle-3.4-all.zip
  
127  Lesson10-Hydration-Reminder/T10.04-Solution-PeriodicSyncWithJobDispatcher/.gitignore
@@ -1,124 +1,41 @@
.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.04-Solution-PeriodicSyncWithJobDispatcher/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.04-Solution-PeriodicSyncWithJobDispatcher/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,7 +19,7 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
    // COMPLETED (1) Add gradle dependency for Firebase Job Dispatcher
    compile 'com.firebase:firebase-jobdispatcher:0.5.2'
}
  
1  ...n-Reminder/T10.04-Solution-PeriodicSyncWithJobDispatcher/app/src/main/AndroidManifest.xml
@@ -1,6 +1,7 @@
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.background">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
  
28  ...patcher/app/src/main/java/com/example/android/background/utilities/NotificationUtils.java
@@ -16,6 +16,7 @@
package com.example.android.background.utilities;

import android.app.Notification;
import android.app.NotificationChannel;
import android.app.NotificationManager;
import android.app.PendingIntent;
import android.content.Context;
@@ -49,6 +50,10 @@
     * This pending intent id is used to uniquely reference the pending intent
     */
    private static final int WATER_REMINDER_PENDING_INTENT_ID = 3417;
    /**
     * This notification channel id is used to link notifications to this channel
     */
    private static final String WATER_REMINDER_NOTIFICATION_CHANNEL_ID = "reminder_notification_channel";
    private static final int ACTION_DRINK_PENDING_INTENT_ID = 1;
    private static final int ACTION_IGNORE_PENDING_INTENT_ID = 14;

@@ -59,8 +64,16 @@ public static void clearAllNotifications(Context context) {
    }

    public static void remindUserBecauseCharging(Context context) {

        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context)
        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            NotificationChannel mChannel = new NotificationChannel(
                    WATER_REMINDER_NOTIFICATION_CHANNEL_ID,
                    context.getString(R.string.main_notification_channel_name),
                    NotificationManager.IMPORTANCE_HIGH);
            notificationManager.createNotificationChannel(mChannel);
            }
        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context,WATER_REMINDER_NOTIFICATION_CHANNEL_ID)
                .setColor(ContextCompat.getColor(context, R.color.colorPrimary))
                .setSmallIcon(R.drawable.ic_drink_notification)
                .setLargeIcon(largeIcon(context))
@@ -74,15 +87,10 @@ public static void remindUserBecauseCharging(Context context) {
                .addAction(ignoreReminderAction(context))
                .setAutoCancel(true);

        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN) {
            notificationBuilder.setPriority(Notification.PRIORITY_HIGH);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN
                && Build.VERSION.SDK_INT < Build.VERSION_CODES.O) {
            notificationBuilder.setPriority(NotificationCompat.PRIORITY_HIGH);
        }


        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);

        /* WATER_REMINDER_NOTIFICATION_ID allows you to update or cancel the notification later on */
        notificationManager.notify(WATER_REMINDER_NOTIFICATION_ID, notificationBuilder.build());
    }
    private static Action ignoreReminderAction(Context context) {
  
1  ...r/T10.04-Solution-PeriodicSyncWithJobDispatcher/app/src/main/res/layout/activity_main.xml
@@ -85,4 +85,5 @@
            android:text="@string/default_charge_notification_count" />

    </RelativeLayout>

</LinearLayout> 
  
1  ...eminder/T10.04-Solution-PeriodicSyncWithJobDispatcher/app/src/main/res/values/strings.xml
@@ -22,5 +22,6 @@

    <string name="charging_reminder_notification_title">Device Charging - Where\'s that water?</string>
    <string name="charging_reminder_notification_body">There\'s never a better time to drink water than when you know your phone is charging. Drink up!</string>
    <string name="main_notification_channel_name">Primary</string>

</resources>
  
5  Lesson10-Hydration-Reminder/T10.04-Solution-PeriodicSyncWithJobDispatcher/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}

  
4  ...er/T10.04-Solution-PeriodicSyncWithJobDispatcher/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
#Fri Jan 13 11:41:03 PST 2017
#Mon Sep 25 13:19:01 AEST 2017
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-3.3-all.zip
distributionUrl=https\://services.gradle.org/distributions/gradle-3.4-all.zip
  
127  Lesson10-Hydration-Reminder/T10.05-Exercise-ChargingBroadcastReceiver/.gitignore
@@ -1,124 +1,41 @@
.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.05-Exercise-ChargingBroadcastReceiver/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.05-Exercise-ChargingBroadcastReceiver/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,6 +19,6 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
    compile 'com.firebase:firebase-jobdispatcher:0.5.2'
}
  
1  ...ation-Reminder/T10.05-Exercise-ChargingBroadcastReceiver/app/src/main/AndroidManifest.xml
@@ -1,6 +1,7 @@
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.background">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
  
1  ...rgingBroadcastReceiver/app/src/main/java/com/example/android/background/MainActivity.java
@@ -107,6 +107,7 @@ public void incrementWater(View view) {
        startService(incrementWaterCountIntent);
    }


    @Override
    protected void onDestroy() {
        super.onDestroy();
  
28  ...eceiver/app/src/main/java/com/example/android/background/utilities/NotificationUtils.java
@@ -16,6 +16,7 @@
package com.example.android.background.utilities;

import android.app.Notification;
import android.app.NotificationChannel;
import android.app.NotificationManager;
import android.app.PendingIntent;
import android.content.Context;
@@ -49,6 +50,10 @@
     * This pending intent id is used to uniquely reference the pending intent
     */
    private static final int WATER_REMINDER_PENDING_INTENT_ID = 3417;
    /**
     * This notification channel id is used to link notifications to this channel
     */
    private static final String WATER_REMINDER_NOTIFICATION_CHANNEL_ID = "reminder_notification_channel";
    private static final int ACTION_DRINK_PENDING_INTENT_ID = 1;
    private static final int ACTION_IGNORE_PENDING_INTENT_ID = 14;

@@ -59,8 +64,16 @@ public static void clearAllNotifications(Context context) {
    }

    public static void remindUserBecauseCharging(Context context) {

        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context)
        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            NotificationChannel mChannel = new NotificationChannel(
                    WATER_REMINDER_NOTIFICATION_CHANNEL_ID,
                    context.getString(R.string.main_notification_channel_name),
                    NotificationManager.IMPORTANCE_HIGH);
            notificationManager.createNotificationChannel(mChannel);
            }
        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context,WATER_REMINDER_NOTIFICATION_CHANNEL_ID)
                .setColor(ContextCompat.getColor(context, R.color.colorPrimary))
                .setSmallIcon(R.drawable.ic_drink_notification)
                .setLargeIcon(largeIcon(context))
@@ -74,15 +87,10 @@ public static void remindUserBecauseCharging(Context context) {
                .addAction(ignoreReminderAction(context))
                .setAutoCancel(true);

        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN) {
            notificationBuilder.setPriority(Notification.PRIORITY_HIGH);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN
                && Build.VERSION.SDK_INT < Build.VERSION_CODES.O) {
            notificationBuilder.setPriority(NotificationCompat.PRIORITY_HIGH);
        }


        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);

        /* WATER_REMINDER_NOTIFICATION_ID allows you to update or cancel the notification later on */
        notificationManager.notify(WATER_REMINDER_NOTIFICATION_ID, notificationBuilder.build());
    }
    private static Action ignoreReminderAction(Context context) {
  
1  ...inder/T10.05-Exercise-ChargingBroadcastReceiver/app/src/main/res/layout/activity_main.xml
@@ -85,4 +85,5 @@
            android:text="@string/default_charge_notification_count" />

    </RelativeLayout>

</LinearLayout> 
  
1  ...on-Reminder/T10.05-Exercise-ChargingBroadcastReceiver/app/src/main/res/values/strings.xml
@@ -22,5 +22,6 @@

    <string name="charging_reminder_notification_title">Device Charging - Where\'s that water?</string>
    <string name="charging_reminder_notification_body">There\'s never a better time to drink water than when you know your phone is charging. Drink up!</string>
    <string name="main_notification_channel_name">Primary</string>

</resources>
  
5  Lesson10-Hydration-Reminder/T10.05-Exercise-ChargingBroadcastReceiver/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}

  
4  ...minder/T10.05-Exercise-ChargingBroadcastReceiver/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
#Fri Jan 13 11:41:03 PST 2017
#Mon Sep 25 13:19:01 AEST 2017
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-3.3-all.zip
distributionUrl=https\://services.gradle.org/distributions/gradle-3.4-all.zip
  
127  Lesson10-Hydration-Reminder/T10.05-Solution-ChargingBroadcastReceiver/.gitignore
@@ -1,124 +1,41 @@
.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.05-Solution-ChargingBroadcastReceiver/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.05-Solution-ChargingBroadcastReceiver/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,6 +19,6 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
    compile 'com.firebase:firebase-jobdispatcher:0.5.2'
}
  
1  ...ation-Reminder/T10.05-Solution-ChargingBroadcastReceiver/app/src/main/AndroidManifest.xml
@@ -1,6 +1,7 @@
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.background">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
  
1  ...rgingBroadcastReceiver/app/src/main/java/com/example/android/background/MainActivity.java
@@ -127,6 +127,7 @@ public void incrementWater(View view) {
        startService(incrementWaterCountIntent);
    }


    @Override
    protected void onDestroy() {
        super.onDestroy();
  
28  ...eceiver/app/src/main/java/com/example/android/background/utilities/NotificationUtils.java
@@ -16,6 +16,7 @@
package com.example.android.background.utilities;

import android.app.Notification;
import android.app.NotificationChannel;
import android.app.NotificationManager;
import android.app.PendingIntent;
import android.content.Context;
@@ -49,6 +50,10 @@
     * This pending intent id is used to uniquely reference the pending intent
     */
    private static final int WATER_REMINDER_PENDING_INTENT_ID = 3417;
    /**
     * This notification channel id is used to link notifications to this channel
     */
    private static final String WATER_REMINDER_NOTIFICATION_CHANNEL_ID = "reminder_notification_channel";
    private static final int ACTION_DRINK_PENDING_INTENT_ID = 1;
    private static final int ACTION_IGNORE_PENDING_INTENT_ID = 14;

@@ -59,8 +64,16 @@ public static void clearAllNotifications(Context context) {
    }

    public static void remindUserBecauseCharging(Context context) {

        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context)
        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            NotificationChannel mChannel = new NotificationChannel(
                    WATER_REMINDER_NOTIFICATION_CHANNEL_ID,
                    context.getString(R.string.main_notification_channel_name),
                    NotificationManager.IMPORTANCE_HIGH);
            notificationManager.createNotificationChannel(mChannel);
            }
        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context,WATER_REMINDER_NOTIFICATION_CHANNEL_ID)
                .setColor(ContextCompat.getColor(context, R.color.colorPrimary))
                .setSmallIcon(R.drawable.ic_drink_notification)
                .setLargeIcon(largeIcon(context))
@@ -74,15 +87,10 @@ public static void remindUserBecauseCharging(Context context) {
                .addAction(ignoreReminderAction(context))
                .setAutoCancel(true);

        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN) {
            notificationBuilder.setPriority(Notification.PRIORITY_HIGH);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN
                && Build.VERSION.SDK_INT < Build.VERSION_CODES.O) {
            notificationBuilder.setPriority(NotificationCompat.PRIORITY_HIGH);
        }


        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);

        /* WATER_REMINDER_NOTIFICATION_ID allows you to update or cancel the notification later on */
        notificationManager.notify(WATER_REMINDER_NOTIFICATION_ID, notificationBuilder.build());
    }
    private static Action ignoreReminderAction(Context context) {
  
1  ...inder/T10.05-Solution-ChargingBroadcastReceiver/app/src/main/res/layout/activity_main.xml
@@ -85,4 +85,5 @@
            android:text="@string/default_charge_notification_count" />

    </RelativeLayout>

</LinearLayout> 
  
1  ...on-Reminder/T10.05-Solution-ChargingBroadcastReceiver/app/src/main/res/values/strings.xml
@@ -22,5 +22,6 @@

    <string name="charging_reminder_notification_title">Device Charging - Where\'s that water?</string>
    <string name="charging_reminder_notification_body">There\'s never a better time to drink water than when you know your phone is charging. Drink up!</string>
    <string name="main_notification_channel_name">Primary</string>

</resources>
  
5  Lesson10-Hydration-Reminder/T10.05-Solution-ChargingBroadcastReceiver/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}

  
4  ...minder/T10.05-Solution-ChargingBroadcastReceiver/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
#Fri Jan 13 11:41:03 PST 2017
#Mon Sep 25 13:19:01 AEST 2017
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-3.3-all.zip
distributionUrl=https\://services.gradle.org/distributions/gradle-3.4-all.zip
  
127  Lesson10-Hydration-Reminder/T10.06-Exercise-StickyBroadcastForCharging/.gitignore
@@ -1,124 +1,41 @@
.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.06-Exercise-StickyBroadcastForCharging/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.06-Exercise-StickyBroadcastForCharging/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,6 +19,6 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
    compile 'com.firebase:firebase-jobdispatcher:0.5.2'
}
  
1  ...tion-Reminder/T10.06-Exercise-StickyBroadcastForCharging/app/src/main/AndroidManifest.xml
@@ -1,6 +1,7 @@
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.background">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
  
1  ...kyBroadcastForCharging/app/src/main/java/com/example/android/background/MainActivity.java
@@ -142,6 +142,7 @@ public void incrementWater(View view) {
        startService(incrementWaterCountIntent);
    }


    @Override
    protected void onDestroy() {
        super.onDestroy();
  
28  ...harging/app/src/main/java/com/example/android/background/utilities/NotificationUtils.java
@@ -16,6 +16,7 @@
package com.example.android.background.utilities;

import android.app.Notification;
import android.app.NotificationChannel;
import android.app.NotificationManager;
import android.app.PendingIntent;
import android.content.Context;
@@ -49,6 +50,10 @@
     * This pending intent id is used to uniquely reference the pending intent
     */
    private static final int WATER_REMINDER_PENDING_INTENT_ID = 3417;
    /**
     * This notification channel id is used to link notifications to this channel
     */
    private static final String WATER_REMINDER_NOTIFICATION_CHANNEL_ID = "reminder_notification_channel";
    private static final int ACTION_DRINK_PENDING_INTENT_ID = 1;
    private static final int ACTION_IGNORE_PENDING_INTENT_ID = 14;

@@ -59,8 +64,16 @@ public static void clearAllNotifications(Context context) {
    }

    public static void remindUserBecauseCharging(Context context) {

        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context)
        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            NotificationChannel mChannel = new NotificationChannel(
                    WATER_REMINDER_NOTIFICATION_CHANNEL_ID,
                    context.getString(R.string.main_notification_channel_name),
                    NotificationManager.IMPORTANCE_HIGH);
            notificationManager.createNotificationChannel(mChannel);
            }
        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context,WATER_REMINDER_NOTIFICATION_CHANNEL_ID)
                .setColor(ContextCompat.getColor(context, R.color.colorPrimary))
                .setSmallIcon(R.drawable.ic_drink_notification)
                .setLargeIcon(largeIcon(context))
@@ -74,15 +87,10 @@ public static void remindUserBecauseCharging(Context context) {
                .addAction(ignoreReminderAction(context))
                .setAutoCancel(true);

        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN) {
            notificationBuilder.setPriority(Notification.PRIORITY_HIGH);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN
                && Build.VERSION.SDK_INT < Build.VERSION_CODES.O) {
            notificationBuilder.setPriority(NotificationCompat.PRIORITY_HIGH);
        }


        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);

        /* WATER_REMINDER_NOTIFICATION_ID allows you to update or cancel the notification later on */
        notificationManager.notify(WATER_REMINDER_NOTIFICATION_ID, notificationBuilder.build());
    }
    private static Action ignoreReminderAction(Context context) {
  
1  ...nder/T10.06-Exercise-StickyBroadcastForCharging/app/src/main/res/layout/activity_main.xml
@@ -85,4 +85,5 @@
            android:text="@string/default_charge_notification_count" />

    </RelativeLayout>

</LinearLayout> 
  
1  ...n-Reminder/T10.06-Exercise-StickyBroadcastForCharging/app/src/main/res/values/strings.xml
@@ -22,5 +22,6 @@

    <string name="charging_reminder_notification_title">Device Charging - Where\'s that water?</string>
    <string name="charging_reminder_notification_body">There\'s never a better time to drink water than when you know your phone is charging. Drink up!</string>
    <string name="main_notification_channel_name">Primary</string>

</resources>
  
5  Lesson10-Hydration-Reminder/T10.06-Exercise-StickyBroadcastForCharging/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}

  
4  ...inder/T10.06-Exercise-StickyBroadcastForCharging/gradle/wrapper/gradle-wrapper.properties
@@ -1,6 +1,6 @@
#Fri Jan 13 11:41:03 PST 2017
#Mon Sep 25 13:19:01 AEST 2017
distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-3.3-all.zip
distributionUrl=https\://services.gradle.org/distributions/gradle-3.4-all.zip
  
127  Lesson10-Hydration-Reminder/T10.06-Solution-StickyBroadcastForCharging/.gitignore
@@ -1,124 +1,41 @@
.DS_Store

# Created by https://www.gitignore.io/api/intellij,android,gradle

### Intellij ###
# Covers JetBrains IDEs: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio and Webstorm
# Reference: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# User-specific stuff:
.idea/workspace.xml
.idea/tasks.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml

# Sensitive or high-churn files:
.idea/dataSources.ids
.idea/dataSources.xml
.idea/dataSources.local.xml
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml

# Gradle:
.idea/gradle.xml
.idea/libraries

# Mongo Explorer plugin:
.idea/mongoSettings.xml

## File-based project format:
*.iws

## Plugin-specific files:

# IntelliJ
/out/

# mpeltonen/sbt-idea plugin
.idea_modules/

# JIRA plugin
atlassian-ide-plugin.xml

# Crashlytics plugin (for Android Studio and IntelliJ)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### Intellij Patch ###
# Comment Reason: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# modules.xml
# .idea/misc.xml 
# *.ipr


### Android ###
# Built application files
# built application files
*.apk
*.ap_

# Files for the ART/Dalvik VM
# files for the dex VM
*.dex

# Java class files
*.class

# Generated files
# generated files
bin/
gen/
out/
gen/

# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Proguard folder generated by Eclipse
proguard/
# Libraries used by the app
# Can explicitly add if we want, but shouldn't do so blindly.  Licenses, bloat, etc.
/libs

# Log Files
*.log

# Android Studio Navigation editor temp files
.navigation/
# Build stuff (auto-generated by android update project ...)
build.xml
ant.properties
local.properties
project.properties

# Android Studio captures folder
captures/
# Eclipse project files
.classpath
.project

# Intellij
# idea project files
.idea/
*.iml
.idea/workspace.xml
.idea/libraries

# Keystore files
*.jks

### Android Patch ###
gen-external-apklibs

*.ipr
*.iws

### Gradle ###
# Gradle-based build
.gradle
/build/

# Ignore Gradle GUI config
gradle-app.setting

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache

# # Work around https://youtrack.jetbrains.com/issue/IDEA-116898
# gradle/wrapper/gradle-wrapper.properties

#removing .idea folder

.idea/ 
build/
  
1  Lesson10-Hydration-Reminder/T10.06-Solution-StickyBroadcastForCharging/app/.gitignore
This file was deleted.

  
10  Lesson10-Hydration-Reminder/T10.06-Solution-StickyBroadcastForCharging/app/build.gradle
@@ -1,12 +1,12 @@
apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    compileSdkVersion 26
    buildToolsVersion "26.0.1"
    defaultConfig {
        applicationId "com.example.android.background"
        minSdkVersion 10
        targetSdkVersion 25
        minSdkVersion 14
        targetSdkVersion 26
        versionCode 1
        versionName "1.0"
    }
@@ -19,6 +19,6 @@ android {

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:25.1.0'
    compile 'com.android.support:appcompat-v7:26.0.1'
    compile 'com.firebase:firebase-jobdispatcher:0.5.2'
}
  
1  ...tion-Reminder/T10.06-Solution-StickyBroadcastForCharging/app/src/main/AndroidManifest.xml
@@ -1,6 +1,7 @@
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.android.background">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
  
1  ...kyBroadcastForCharging/app/src/main/java/com/example/android/background/MainActivity.java
@@ -150,6 +150,7 @@ public void incrementWater(View view) {
        startService(incrementWaterCountIntent);
    }


    @Override
    protected void onDestroy() {
        super.onDestroy();
  
28  ...harging/app/src/main/java/com/example/android/background/utilities/NotificationUtils.java
@@ -16,6 +16,7 @@
package com.example.android.background.utilities;

import android.app.Notification;
import android.app.NotificationChannel;
import android.app.NotificationManager;
import android.app.PendingIntent;
import android.content.Context;
@@ -49,6 +50,10 @@
     * This pending intent id is used to uniquely reference the pending intent
     */
    private static final int WATER_REMINDER_PENDING_INTENT_ID = 3417;
    /**
     * This notification channel id is used to link notifications to this channel
     */
    private static final String WATER_REMINDER_NOTIFICATION_CHANNEL_ID = "reminder_notification_channel";
    private static final int ACTION_DRINK_PENDING_INTENT_ID = 1;
    private static final int ACTION_IGNORE_PENDING_INTENT_ID = 14;

@@ -59,8 +64,16 @@ public static void clearAllNotifications(Context context) {
    }

    public static void remindUserBecauseCharging(Context context) {

        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context)
        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            NotificationChannel mChannel = new NotificationChannel(
                    WATER_REMINDER_NOTIFICATION_CHANNEL_ID,
                    context.getString(R.string.main_notification_channel_name),
                    NotificationManager.IMPORTANCE_HIGH);
            notificationManager.createNotificationChannel(mChannel);
            }
        NotificationCompat.Builder notificationBuilder = new NotificationCompat.Builder(context,WATER_REMINDER_NOTIFICATION_CHANNEL_ID)
                .setColor(ContextCompat.getColor(context, R.color.colorPrimary))
                .setSmallIcon(R.drawable.ic_drink_notification)
                .setLargeIcon(largeIcon(context))
@@ -74,15 +87,10 @@ public static void remindUserBecauseCharging(Context context) {
                .addAction(ignoreReminderAction(context))
                .setAutoCancel(true);

        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN) {
            notificationBuilder.setPriority(Notification.PRIORITY_HIGH);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.JELLY_BEAN
                && Build.VERSION.SDK_INT < Build.VERSION_CODES.O) {
            notificationBuilder.setPriority(NotificationCompat.PRIORITY_HIGH);
        }


        NotificationManager notificationManager = (NotificationManager)
                context.getSystemService(Context.NOTIFICATION_SERVICE);

        /* WATER_REMINDER_NOTIFICATION_ID allows you to update or cancel the notification later on */
        notificationManager.notify(WATER_REMINDER_NOTIFICATION_ID, notificationBuilder.build());
    }
    private static Action ignoreReminderAction(Context context) {
  
1  ...nder/T10.06-Solution-StickyBroadcastForCharging/app/src/main/res/layout/activity_main.xml
@@ -85,4 +85,5 @@
            android:text="@string/default_charge_notification_count" />

    </RelativeLayout>

</LinearLayout> 
  
1  ...n-Reminder/T10.06-Solution-StickyBroadcastForCharging/app/src/main/res/values/strings.xml
@@ -22,5 +22,6 @@

    <string name="charging_reminder_notification_title">Device Charging - Where\'s that water?</string>
    <string name="charging_reminder_notification_body">There\'s never a better time to drink water than when you know your phone is charging. Drink up!</string>
    <string name="main_notification_channel_name">Primary</string>

</resources>
  
5  Lesson10-Hydration-Reminder/T10.06-Solution-StickyBroadcastForCharging/build.gradle
@@ -5,7 +5,7 @@ buildscript {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.3'
        classpath 'com.android.tools.build:gradle:2.3.3'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
@@ -19,6 +19,9 @@ allprojects {
    }
    repositories {
        jcenter()
        maven {
            url "https://maven.google.com"
        }
    }
}
